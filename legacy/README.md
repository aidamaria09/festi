# Legacy version

The original FESTI project: four static HTML pages (Romanian), inline CSS, and vanilla JS with `localStorage` for the forum and favorites. Kept here for reference — see the [root README](../README.md) for the current version and what changed.
